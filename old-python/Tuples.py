
coordinates = (4, 5) # nếu muốn có tuples thì phải có dấu (), tuples ko thể chỉnh sửa, nó là độc nhất
print(coordinates[0]) # thứ tự index giống như của list (bắt đầu từ 0)

coordinates1 = [(5, 6), (7, 9), (10, 5)] #có thể bỏ tuples trong list (mỗi tuples là 1 giá trị trong list)
print(coordinates1)